<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera captures for note images live in the cache; they're uploaded then forgotten. -->
    <cache-path name="camera" path="camera/" />
</paths>
